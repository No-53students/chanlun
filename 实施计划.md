# 缠论学习项目 · 实施计划

> 本文件是项目的总计划，用于在步骤中断后快速恢复上下文。**原文 `缠论原文/` 只读，永不改动。**

## 一、项目目标

1. 把缠论 108 课重构为一份"完整、易学"的新讲义——术语自底向上、图解、原文出处、练习，解决原文"散、乱、术语悬空"的问题。
2. 开发一个 H5 可视化学习工具（Vue + ECharts），分两阶段落地。

## 二、目录结构约定

```
c:\wgr_self\canlun\
├── 缠论原文/        # 原文，只读，永不改动
├── 缠论大纲/        # 已有：全书 108 课逐课大纲
├── 缠论讲义/        # 新：重构文档（自底向上）
│   ├── 00-章节骨架.md    # 公共契约：章节 + 术语依赖顺序
│   ├── 01-术语表.md      # 公共契约：术语精确定义
│   ├── 02-...md          # 各章节（按骨架）
│   └── assets/           # 图解 / 示例数据（与 H5 复用）
├── h5/              # 新：Vue 可视化应用
└── 实施计划.md       # 本文件
```

## 三、技术栈

- **文档**：Markdown（GitHub 风格，VSCode 可预览）。
- **H5**：Vue 3 + Vite + ECharts（candlestick）。
- **数据源**：阶段一用内置示例 + CSV 导入；阶段二加 Node.js 数据代理（绕 CORS + 缓存行情）。

## 四、实施步骤

### 步骤 0 — 记录本计划（✅ 完成）
产出本文件。

### 步骤 1 — 定"章节骨架 + 术语表"（公共契约）（进行中）
- 产出 `缠论讲义/00-章节骨架.md`：章节目录 + 每章对应原文课号 + 核心要点。
- 产出 `缠论讲义/01-术语表.md`：术语精确定义 + 课号 + 常见误区。
- 作用：文档按骨架撰写，H5 按骨架划分模块、按术语表实现算法。**两边共用，是并行推进的耦合点。**

### 步骤 2 — 撰写讲义核心章节（文档并行推进）
- 按骨架逐章撰写。每章固定结构：定义 → 动机 → 图解 → 原文出处 → 常见误区 → 练习。
- 图解先用文字/ASCII 示意，正式图交给 H5 复用（一图两用）。

### 步骤 3 — H5 阶段一：教学演示 + CSV 导入自动标注
- Vue3 + Vite 骨架 + ECharts K 线渲染。
- 标注层：分型 / 笔 / 线段 / 中枢 / 买卖点。
- **核心算法**：分型 → 笔 → 线段 → 中枢递归 → 背驰/买卖点（实现时约定规则并记录，可迭代）。
- 交互教学模块：概念页 + 可点选示例。
- CSV 导入 → 自动标注。

### 步骤 4 — H5 阶段二：输入股票代码分析
- Node.js 数据代理（绕 CORS + 缓存行情）。
- 输入股票代码 → 拉实时 K 线 → 自动分析展示。

## 五、关键约定

1. **原文只读**，永不改动；所有新产出放 `缠论讲义/` 与 `h5/`。
2. 术语以《01-术语表》为准，文档与 H5 算法共用同一套定义。
3. 算法歧义（旧笔/新笔、线段破坏的两种情况）在实现时**明确约定规则并写入代码注释/文档**。
4. 图解一图两用：文档引用的图 = H5 渲染的同一份数据。

## 六、进度记录

| 步骤 | 状态 |
|---|---|
| 0 记录计划 | ✅ |
| 1 章节骨架 + 术语表 | 进行中 |
| 2 讲义章节 | 待开始 |
| 3 H5 阶段一 | 待开始 |
| 4 H5 阶段二 | 待开始 |
